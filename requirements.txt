# No LLM SDK required for the default (Ollama) provider -- it's called over
# plain HTTP via the standard library. pytest is optional; the smoke tests
# also run standalone with `python3 tests/test_tools_smoke.py`.
pytest>=7.0
